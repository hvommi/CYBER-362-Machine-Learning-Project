#import Kmeans   
from sklearn.cluster import KMeans
from sklearn.cluster import DBSCAN
#define the model   
clustermodel = DBSCAN(min_samples=10)
#create clusters
dsc = clustermodel.fit(df.to_numpy())
pred_y = clustermodel.fit_predict(df)
df['test'] = pred_y
import seaborn as sns

#pair plot to show cluserting vs CLASS_LABEL
sns.pairplot(df, hue='CLASS_LABEL', diag_kind='hist')
plt.savefig('C:/users/Andrew/desktop/test.png')
